export enum AccountType {
  Individual = "INDIVIDUAL",
  Organization = "ORGANIZATION",
}

export enum Role {
  Admin = "admin",
  Read = "read",
  Write = "write",
}

export enum FolderType{
  Folder = "FOLDER",
  File = "FILE"
}
